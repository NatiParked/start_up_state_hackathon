# Technology Stack

> Auto-generated by /spec:new-project on 2026-05-09

## Languages

**Primary:**
- JavaScript (ES Modules) — Frontend (`goed/src/**/*.js`, `goed/src/**/*.vue`) and Supabase Edge Functions (`supabase/functions/**/*.js`). Locked decision: JS-only (no TypeScript).
- Vue Single-File Components — UI components in `goed/src/components/`, `goed/src/views/`, `goed/src/App.vue`
- SQL (PostgreSQL dialect) — Database migrations in `supabase/migrations/*.sql`

**Secondary:**
- HTML — `goed/index.html` (single Vite entry point)
- CSS (PostCSS + Tailwind) — `goed/src/styles/brand.css`
- Bash — `scripts/overnight-run.sh`, `scripts/test-playwright.sh`

## Runtime

**Environment:**
- Node.js `^20.19.0 || >=22.12.0` (declared in `goed/package.json` `engines` field). Locally installed: v25.5.0.
- Deno (managed by Supabase) — runtime for Edge Functions in `supabase/functions/`. Functions use `Deno.serve()` and `Deno.env.get()`.
- Browser — modern evergreen browsers via Vite ESM bundles.

**Package Manager:**
- npm 11.8.0
- Lockfile: `goed/package-lock.json` (157KB, present and committed)
- Single `package.json` lives in `goed/` (not at repo root). The repo is a hybrid layout: `goed/` is the npm app, `supabase/` is a sibling tree with no `package.json` (Deno imports use `npm:` URL specifier, e.g. `import { createClient } from 'npm:@supabase/supabase-js@2'`).

## Frameworks

**Core:**
- Vue `^3.5.32` — Frontend SPA framework (Composition API; see `goed/src/main.js`)
- Vue Router `^5.0.4` — Client-side routing with lazy-loaded views (`goed/src/router/index.js`)
- Pinia `^3.0.4` — State management; stores in `goed/src/stores/` (`admin.js`, `filters.js`, `startups.js`)
- Tailwind CSS `^3.4.19` — Utility-first styling (`goed/tailwind.config.js`)

**Testing:**
- Playwright (via MCP) — Browser automation/verification, configured in `.mcp.json` (chromium, headless, no-sandbox). Used by `/spec:verify-phase` and `scripts/overnight-run.sh`.
- No unit test framework (Vitest, Jest) is installed. Verification is end-to-end via Playwright MCP only.

**Build/Dev:**
- Vite `^8.0.8` — Dev server and production bundler (`goed/vite.config.js`)
- `@vitejs/plugin-vue` `^6.0.6` — Vue SFC support
- `vite-plugin-vue-devtools` `^8.1.1` — In-browser Vue Devtools panel
- PostCSS `^8.5.14` + Autoprefixer `^10.5.0` — CSS pipeline (`goed/postcss.config.js`)

## Key Dependencies

| Dependency | Version | Purpose |
|------------|---------|---------|
| `vue` | ^3.5.32 | Reactive UI framework (Composition API throughout) |
| `vue-router` | ^5.0.4 | SPA routing; uses `createWebHistory()` and lazy `() => import()` views |
| `pinia` | ^3.0.4 | Global state stores (`storeToRefs` pattern used in views) |
| `@supabase/supabase-js` | ^2.105.4 | DB client + auth — singleton in `goed/src/lib/supabase.js`; also used in Edge Functions via `npm:@supabase/supabase-js@2` |
| `gsap` | ^3.15.0 | Entrance animations on landing/portal cards |
| `ol` | ^10.9.0 | OpenLayers map engine — direct imports for `Style`, `Circle`, `Fill`, `Stroke`, `Text` (see `goed/src/lib/clusterStyle.js`) |
| `vue3-openlayers` | ^12.2.2 | Vue 3 wrapper around OpenLayers; registered globally in `goed/src/main.js`; CSS imported from `vue3-openlayers/vue3-openlayers.css` |
| `tailwindcss` | ^3.4.19 | Styling; custom theme colors `utah-blue`, `utah-blue-dark`, `hiring-green`, `error-red`, `warning-yellow` |
| `autoprefixer` | ^10.5.0 | CSS vendor prefix injection |
| `postcss` | ^8.5.14 | CSS processing pipeline |
| `vite` | ^8.0.8 | Dev/build tooling |
| `@vitejs/plugin-vue` | ^6.0.6 | Vue SFC compilation |
| `vite-plugin-vue-devtools` | ^8.1.1 | DX: in-app Vue inspector |

## Configuration

**Environment:**
- Frontend: `goed/.env.local` (gitignored), template at `goed/.env.example`
  - `VITE_SUPABASE_URL` — Supabase project URL (exposed to browser via `import.meta.env`)
  - `VITE_SUPABASE_ANON_KEY` — Supabase anon JWT
  - `VITE_LOGO_DEV_TOKEN` — logo.dev API token for company logos
- Edge Functions: secrets set via `supabase secrets set ...` (read with `Deno.env.get()`)
  - `SUPABASE_URL` (auto-injected)
  - `SUPABASE_SERVICE_ROLE_KEY` (auto-injected)
  - `GOOGLE_AI_API_KEY` — Google Gemini API key (single key for all AI; locked decision)
  - `GOOGLE_PLACES_API_KEY` — Google Places (New) API
  - `LOGO_DEV_TOKEN` — logo.dev (server-side mirror)
  - `NEWS_API_KEY` — NewsAPI.org (stretch enricher)
  - `WAPPALYZER_API_KEY` — Wappalyzer tech-stack (stretch)
  - `PRODUCTHUNT_API_TOKEN` — ProductHunt GraphQL (stretch)

**Build:**
- `goed/vite.config.js` — Vue + DevTools plugins, `@` alias → `./src`
- `goed/jsconfig.json` — Editor IntelliSense for `@/*` path alias
- `goed/tailwind.config.js` — Content globs `./index.html`, `./src/**/*.{vue,js}`; custom Utah palette
- `goed/postcss.config.js` — Tailwind + Autoprefixer plugins
- `netlify.toml` (repo root) — Build base `goed/`, publish `dist`, command `npm run build`, SPA redirect `/* → /index.html 200`

## Database & Storage

**Databases:**
- Supabase (Postgres 15+ hosted)
  - Connection: `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` (frontend), `SUPABASE_SERVICE_ROLE_KEY` (Edge Functions)
  - Client: `@supabase/supabase-js` v2; singleton at `goed/src/lib/supabase.js`; admin client helper at `supabase/functions/_shared/supabaseAdmin.js`
  - Migrations: `supabase/migrations/0001_init.sql` … `0006_admin_users.sql`
  - All Map-product tables prefixed `map_` (project convention): `map_startups`, `map_startup_submissions`, `map_admin_users`, `map_refresh_log`
  - RLS enabled on `map_admin_users` (authenticated SELECT only; no client-side INSERT/DELETE)

**Postgres Extensions:**
- `pg_cron` — scheduled jobs (weekly `refresh-jobs` invocation)
- `pg_net` — `net.http_post` for cron → Edge Function calls
- `pgvector` (planned per memory) — Navigator semantic search uses `vector(768)` for Gemini embeddings (not 1536)

**File Storage:**
- Local filesystem only (no Supabase Storage usage detected)
- Logo URLs are remote references to `img.logo.dev` (not stored)

**Caching:**
- None (no Redis, no Cloudflare KV)
- Supabase HTTP-level caching via REST PostgREST defaults

## External Integrations

**APIs & Services:**
- **Google Gemini** (`generativelanguage.googleapis.com/v1beta/models`) — All AI calls; default model `gemini-2.0-flash`
  - Helper: `supabase/functions/_shared/llm.js` (`callLLM()` with JSON Schema → Gemini Schema converter)
  - Auth: `GOOGLE_AI_API_KEY` env
- **Google Places (New)** (`places.googleapis.com/v1/places:searchText`) — Phone, rating, business status enrichment
  - Helper: `supabase/functions/_shared/google-places.js`
  - Auth: `GOOGLE_PLACES_API_KEY` env (stretch — silent null on missing key)
- **Nominatim / OpenStreetMap** (`nominatim.openstreetmap.org/search`) — Geocoding addresses → lat/lng
  - Helper: `supabase/functions/_shared/nominatim.js` (rate-limited 1 req/sec per ToS; retries on HTTP 429)
  - Auth: none required; identifies via User-Agent
- **logo.dev** (`img.logo.dev/{domain}`) — Company logo URL builder (no HTTP call from server; client resolves directly)
  - Helper: `supabase/functions/_shared/logo-dev.js`
  - Auth: `LOGO_DEV_TOKEN` (server) / `VITE_LOGO_DEV_TOKEN` (client)
- **GitHub Search API** (`api.github.com/search/repositories`) — Org repo count for tech-credibility signal
  - Helper: `supabase/functions/_shared/enrichers/github.js`
  - Auth: none (unauthenticated rate limit)
- **Crunchbase** (scraper) — Investors, total raised, funding rounds
  - Helper: `supabase/functions/_shared/enrichers/crunchbase.js`
- **Utah DCC** (`secure.utah.gov/bes/index.html`) — Utah Division of Corporations registry lookup (HTML scrape)
  - Helper: `supabase/functions/_shared/enrichers/utah-dcc.js`
- **NewsAPI** (`newsapi.org/v2/everything`) — Recent press headlines (stretch enricher; key-gated)
  - Auth: `NEWS_API_KEY`
- **Wappalyzer** (`api.wappalyzer.com/v2/lookup/`) — Tech stack detection (stretch; key-gated)
  - Auth: `WAPPALYZER_API_KEY`
- **ProductHunt GraphQL** (`api.producthunt.com/v2/api/graphql`) — Launch data (stretch)
  - Auth: `PRODUCTHUNT_API_TOKEN`
- **ATS pollers** (Greenhouse `boards-api.greenhouse.io`, Lever, Ashby) — Live hiring data
  - Helper: `supabase/functions/_shared/ats.js`
  - Auth: none (public job boards)

**Tooling MCP servers (`.mcp.json`):**
- Playwright MCP — Headless Chromium browser automation for `/spec:verify-phase`
- Supabase MCP (per memory) — Schema introspection, migration application, log retrieval

## Platform Requirements

**Development:**
- Node.js `^20.19.0 || >=22.12.0`
- npm 11+
- Supabase CLI — for local stack, migrations (`supabase db push`), function deploy
- Chromium binary at `/home/cayden/.cache/ms-playwright/chromium-1217/chrome-linux64/chrome` (configured in `.mcp.json`)
- WSL2 / Linux preferred (the overnight-run system targets bash)

**Production:**
- Frontend: Netlify (static SPA)
  - Base: `goed/`, publish: `dist`, build: `npm run build`
  - SPA fallback redirect configured in `netlify.toml`
- Backend: Supabase (managed Postgres + Deno Edge Functions)
  - Functions deployed: `onboard-company` (public), `refresh-jobs` (service-role / cron-invoked)
  - Cron: weekly `refresh-jobs` via `pg_cron` + `pg_net.http_post` (manual schedule registration in SQL editor)

---

*Stack analysis: 2026-05-09*
