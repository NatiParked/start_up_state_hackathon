# Technology Stack

> Auto-generated by /spec:new-project on 2026-05-08

## Languages

**Primary:**
- JavaScript (ES Modules) — All application code (`goed/src/*.js`, `.vue` SFC `<script setup>` blocks)
- Vue Single-File Components (`.vue`) — Component templates, scripts, scoped styles (e.g., `goed/src/App.vue`)
- HTML5 — Single entry document `goed/index.html`

**Secondary:**
- CSS (scoped, inside `.vue` `<style scoped>` blocks) — Component styling until Tailwind is added
- SQL (planned) — Supabase schema definitions (see `STARTUP_MAP.md` and `FOUNDERS_NAVIGATOR.md` for `create table` statements)

**Note:** TypeScript is not used anywhere in this project — not in the Vue app and not in Supabase Edge Functions (even though Deno supports TypeScript). All files are `.js`. The project uses `jsconfig.json` (`goed/jsconfig.json`) for path-alias IntelliSense only.

## Runtime

**Environment:**
- Node.js — Required engine declared in `goed/package.json`: `"node": "^20.19.0 || >=22.12.0"`
- Browser runtime — Vite serves an ES module bundle (`goed/index.html` loads `/src/main.js` as `type="module"`)

**Package Manager:**
- npm — Lockfile present at `goed/package-lock.json` (96763 bytes)
- No `pnpm-lock.yaml` or `yarn.lock` detected
- `node_modules/` already installed at `goed/node_modules/`

**Module System:**
- ESM — `"type": "module"` declared in `goed/package.json`

## Frameworks

**Core:**
- Vue 3.5 (`vue@^3.5.32`) — Composition API frontend framework. Bootstrapped in `goed/src/main.js` via `createApp(App)`.
- Vue Router 5 (`vue-router@^5.0.4`) — Client-side routing. Initialized in `goed/src/router/index.js` with `createWebHistory`. Routes array is currently empty; planned routes per `PROJECT_CONTEXT.md`: `/`, `/navigator`, `/map`.
- Pinia 3 (`pinia@^3.0.4`) — State management. Registered in `goed/src/main.js` via `createPinia()`. The `goed/src/stores/` directory exists but is empty (no stores defined yet).

**Testing:**
- Not detected — No test framework installed. `.gitignore` references Cypress and Vitest patterns (`/cypress/videos/`, `__screenshots__/`) suggesting one may be added later, but no config or dependency exists.

**Build/Dev:**
- Vite 8 (`vite@^8.0.8`) — Build tool and dev server. Config at `goed/vite.config.js`.
- `@vitejs/plugin-vue@^6.0.6` — Vue SFC support for Vite.
- `vite-plugin-vue-devtools@^8.1.1` — In-browser Vue devtools panel during dev.

**npm Scripts (`goed/package.json`):**
- `npm run dev` — Start Vite dev server with HMR
- `npm run build` — Production build (outputs to `goed/dist/`, gitignored)
- `npm run preview` — Preview production build locally

## Key Dependencies

### Installed (`goed/package.json`)

| Dependency | Version | Purpose |
|------------|---------|---------|
| `vue` | ^3.5.32 | Core reactive frontend framework |
| `vue-router` | ^5.0.4 | SPA routing between Landing, Navigator, Map pages |
| `pinia` | ^3.0.4 | Cross-component state (intake form, filter state, results) |
| `vite` | ^8.0.8 | Dev server, HMR, production bundler |
| `@vitejs/plugin-vue` | ^6.0.6 | Compile `.vue` SFCs through Vite |
| `vite-plugin-vue-devtools` | ^8.1.1 | Embedded Vue devtools UI in dev |

### Planned (per `PROJECT_CONTEXT.md`, `FOUNDERS_NAVIGATOR.md`, `STARTUP_MAP.md`)

| Dependency | Purpose | Notes |
|------------|---------|-------|
| `tailwindcss` | Utility-first styling across the app | Locked in stack table; not yet added |
| `@supabase/supabase-js` | Supabase client (auth, table CRUD, Edge Functions, pgvector queries) | Required for both products |
| `gsap` | Animations: GSAP entrance on landing cards, intake step transitions, results stagger, Map drawer slide-in | Locked in stack table |
| `vue3-openlayers` | Vue 3 wrapper around OpenLayers for the Startup Map (Product 2 only) | Components: `ol-map`, `ol-view`, `ol-layer-tile`, `ol-source-osm`, `ol-vector-layer`, `ol-feature`, `ol-style`, `ol-overlay`, `ol-interaction-select` |
| `ol` | OpenLayers core (peer of `vue3-openlayers`) | Install command in `STARTUP_MAP.md`: `npm install vue3-openlayers ol` |

## Configuration

**Path Aliases:**
- `@` → `./src` — Configured in both `goed/vite.config.js` (runtime, via `fileURLToPath`) and `goed/jsconfig.json` (editor IntelliSense via `paths`)

**Vite Config (`goed/vite.config.js`):**
- Plugins: `@vitejs/plugin-vue`, `vite-plugin-vue-devtools`
- Resolve alias `@` → `src/`
- No environment-specific config blocks defined

**Editor/IntelliSense (`goed/jsconfig.json`):**
- Excludes `node_modules`, `dist`
- Defines `@/*` path mapping for editor only

**Environment:**
- No `.env`, `.env.example`, or `.env.local` detected at project root or in `goed/`
- `.gitignore` excludes `*.local` (so `.env.local` will be ignored when added)
- Planned env vars (per docs):
  - `VITE_SUPABASE_URL` (anticipated)
  - `VITE_SUPABASE_ANON_KEY` (anticipated)
  - Server-side secrets (Claude/Anthropic API key, geocoding key) live in Supabase Edge Function environment, not the client

**Build:**
- No custom `build` config — relies on Vite defaults
- Output: `goed/dist/` (gitignored)
- No SSR/SSG configuration

## Database & Storage

**Database:**
- Supabase (PostgreSQL) — Planned, not yet provisioned in this repo
  - Connection: via `@supabase/supabase-js` client (env vars `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
  - Extensions required:
    - `pgvector` — Semantic search over resource embeddings (Founder's Navigator). Column: `embedding vector(1536)` on `resources` table.
    - PostGIS or plain `float8` lat/lng columns — Geospatial data for the Startup Map (`STARTUP_MAP.md` schema uses `lat float8`, `lng float8`)
  - Tables planned (see `FOUNDERS_NAVIGATOR.md` and `STARTUP_MAP.md`):
    - `resources` — 100 rows imported from Google Sheet, with auto-generated embeddings
    - `contacts` — GOED staff directory (later stage)
    - `startups` — 96 imported companies, geocoded
    - `startup_submissions` — Pending self-service submissions (`status: pending|approved|rejected`)

**Edge Functions (Supabase, planned):**
- Location: `supabase/functions/<name>/index.js` at **repo root** (not inside `goed/`)
- Language: JavaScript (`.js`) — Supabase CLI initialized from repo root
- `embed-resources` — Generate/refresh embeddings on resource row update
- `search-resources` — Hybrid SQL filter + pgvector cosine similarity
- `generate-next-steps` — Claude reranking + markdown next-steps guide
- `geocode-submission` — Geocode submitted addresses before insert into `startup_submissions`

**File Storage:**
- Supabase Storage (implied by `photos text[]` field in `startups` schema) — For startup photo galleries on self-service submissions
- No local filesystem storage in the app

**Caching:**
- None configured. Map filters explicitly client-side per `STARTUP_MAP.md` ("96 rows is small, instant filtering, no edge function needed").

## External Integrations

**APIs & Services:**

| Service | Purpose | Auth/Access |
|---------|---------|-------------|
| Supabase | Database, auth, Edge Functions, pgvector, file storage | `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (client); service role key in Edge Functions |
| Anthropic Claude | Reranking top resource candidates + writing personalized next-steps guide (Founder's Navigator) | API key in Supabase Edge Function env |
| OpenAI (or Claude) Embeddings | 1536-dim embeddings for resource descriptions (`embedding vector(1536)`) | API key in Supabase Edge Function env |
| Nominatim (OpenStreetMap) | Primary geocoder for addresses → lat/lng (free, no key) | Public endpoint |
| Google Maps Geocoding API | Fallback geocoder if Nominatim accuracy is poor for Utah addresses | Google API key (server-side only) |
| OpenStreetMap Tiles | Base map tiles via `ol-source-osm` | Public, no key |
| Google Sheets (one-time) | Source of truth for initial resource (100 rows) and startup (96 rows) imports | One-time CSV export, no runtime integration |

**Sheet Sources (one-time imports):**
- Resources: `https://docs.google.com/spreadsheets/d/1AdfJ9TDWdICQuzoYQn-6cBmUkOVXWD8mTqJNDnuKD-E/edit`
- Startups: `https://docs.google.com/spreadsheets/d/1D9CUtXpyPubOkt51wD9SDCpglkQv6W6oa33iTs73cCk/edit`

## Platform Requirements

**Development:**
- Node.js `^20.19.0 || >=22.12.0` (from `goed/package.json` `engines`)
- npm (lockfile is `package-lock.json`)
- Modern browser with ES module support
- Recommended IDE: VS Code + Vue (Official) extension (Volar) — per `goed/README.md`
- Recommended browser extensions: Vue.js devtools — per `goed/README.md`

**Production:**
- Hosting: Netlify — Locked per `PROJECT_CONTEXT.md` stack table
  - Static site deploy of `goed/dist/`
  - SPA fallback routing required (planned routes: `/`, `/navigator`, `/map`)
- Backend: Supabase managed Postgres + Edge Functions (separate deployment)
- Custom domain target: Potential live deployment on `startup.utah.gov` (per `PROJECT_CONTEXT.md`)

**Brand/Design Constraints (from `PROJECT_CONTEXT.md`):**
- Utah blue `#0065A4` + white (matches startup.utah.gov brand)
- Performance / polish weighted heavily in judging (25% technical execution, 25% design)

---

*Stack analysis: 2026-05-08*
